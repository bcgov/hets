package pages.app

import geb.Page
import extensions.ReactJSAware

class TemplatePage extends Page implements ReactJSAware {
    static at = { reactReady && title == "" }
    static url = ""

    static content = {
            
    }
}