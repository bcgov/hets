@{
    ViewBag.Title = "HETSAPI - Api Home";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>    
    
    <style>
        body {
            background-color: #fff;
            padding: 10px;
            font-family: Verdana, Geneva, sans-serif;
            font-size: 12pt;
        }      
    </style>
    
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>
    
    
    <script language="javascript">
        function updateUserId(userId) {
            userId = userId.trim();

            if (userId.length > 0) {
                window.location.href = "/api/authentication/dev/token/" + userId;
            }            
        }

        function expireUserId() {
            window.location.href = "/api/authentication/dev/cleartoken";
        }

        function recalcSeniority() {
            var regionId = $("#regionId").value;
            alert('Recalc Seniority for Region Id: ' + regionId);

            $.ajax({
                url: "/api/equipment/recalcSeniority?region=" + regionId,
                type: "GET",
                timeout: 60000, // sets timeout to 1 min
                success: function (response) {
                    alert('done');
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    alert(xhr.responseText);
                }
            });
        }

        function recalcRotation() {
            var requestId = $("#requestId").value;
            alert('Recalc Rotation for Request Id: ' + requestId);

            $.ajax({
                url: "/api/rentalrequests/recalcRotation?rentalRequest=" + requestId,
                type: "GET",
                timeout: 60000, // sets timeout to 1 min
                success: function (response) {
                    alert('done');
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    alert(xhr.responseText);
                }
            });
        }        
    </script>
</head>
<body>
<h2>@ViewData["Title"]</h2>
<hr/>
<div>
    <h3>Current User Id</h3>
    <label>UserId:</label> &nbsp; @Model.UserId
</div>

@if (Model.DevelopmentEnvironment)
{
    <hr/>
    <div>
        <h3>Reset User Id</h3>
        <label>UserId:</label> <input text="" id="newUserId"/>
        <button onclick="updateUserId(newUserId.value);">Override User Context</button>&nbsp;&nbsp;
        <button onclick="expireUserId();">Clear User Context</button>
    </div>
}

@if (Model.DevelopmentEnvironment)
{
    <hr/>
    <div>
        <h3>View HETS API</h3>
        <a href="~/swagger/ui/index.html">Swagger</a>
    </div>

    <hr/>
    <div>
        <h3>Import BC Bid Data</h3>
        <a href="~/Import">Import</a>
    </div>

    <hr/>
    <div>
        <h3>Hangfire Monitor</h3>
        <a href="~/hangfire">Hangfire</a>
    </div>

    <hr/>
    <div>
        <h3>Recalc Equipment Seniority</h3>
        <input type="number" width="5" value="3" id="regionId" /> <button onclick="recalcSeniority();">Recalc (Region (3) "Northern")</button>
    </div>

    <hr/>
    <div>
        <h3>Recalc Rotation List (for a Rental Request)</h3>
        <input type="number" width="5" value="18" id="requestId" /> <button onclick="recalcRotation();">Recalc (Request (18))</button>
        
        <button onclick="getRotationList();">Clear User Context</button>
    </div>
}
</body>
</html>